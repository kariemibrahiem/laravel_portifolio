/**
 * Web Application JavaScript
 * Pure vanilla JavaScript - No React, no build system required
 */

(function() {
  'use strict';

  // Initialize the application when DOM is ready
  document.addEventListener('DOMContentLoaded', function() {
    console.log('Web application initialized');
    
    // Add your vanilla JS initialization here
    // Example: smooth scrolling, event listeners, etc.
  });

  // Utility functions can be added here as needed
  window.webApp = {
    version: '1.0.0',
    init: function() {
      console.log('Web app ready');
    }
  };

  // Initialize
  window.webApp.init();
})();
